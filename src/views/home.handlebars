<h1>Mis Productitos</h1>

<div>
    {{#each productos}}
        <div>
            <p> {{this.title}} </p>
            <p> {{this.description}} </p>
            <p> {{this.price}} </p>
            <button> Comprar </button>
        </div>
    {{/each}}

    {{#if hasPrevPage}}
        <a href="/products?page={{prevPage}}">Anterior</a>
    {{/if}}
    
    <p>Página {{currentPage}} de {{totalPages}}</p>
    
    {{#if hasNextPage}}
        <a href="/products?page={{nextPage}}">Siguiente</a>
    {{/if}}
</div>

